/* 
  beforeAll(fn, timeout)
  beforeEach(fn, timout)
*/

// The beforeAll method runs before all tests
beforeAll(() => {
  // Connect to the database
  // check the the database is not empty
  // prepare everything before testing
})

// The beforeEach method runs before each test
beforeEach(() => {});

// The afterAll method runs after all done
afterAll(() => {
  // Clean data base
  // Clean cache
})